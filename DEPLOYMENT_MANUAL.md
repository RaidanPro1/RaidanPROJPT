
# 🇾🇪 RaidanPro: Field Operations Manual (v2.1)
**تصنيف الوثيقة:** سري للغاية (Top Secret) - للإداريين فقط
**تاريخ الإصدار:** 2026

## 1. مقدمة (Introduction)
نظام **RaidanPro** هو منصة سيادية للذكاء الاصطناعي وإدارة البيانات، صممت خصيصاً للعمل داخل الجمهورية اليمنية مع الالتزام الصارم بالقوانين المحلية (قانون الصحافة 1990، قانون الجرائم والعقوبات 1994). يجمع النظام بين قوة النماذج المحلية (Ollama) وسرعة النماذج السحابية (Gemini) في بنية هجينة.

---

## 2. بروتوكول التثبيت (Installation Protocol)

### المتطلبات المسبقة (Prerequisites)
*   نظام تشغيل: Ubuntu 24.04 LTS (موصى به).
*   الموارد: 16GB RAM (الحد الأدنى)، 8 vCPU، 200GB SSD.
*   GPU: NVIDIA GPU (16GB VRAM) لتشغيل النماذج المحلية بكفاءة.

### خطوات النشر (Deployment Steps)

1.  **استنساخ المستودع (Clone):**
    ```bash
    git clone https://github.com/RaidanPro/sovereign-core.git
    cd sovereign-core
    ```

2.  **تكوين البيئة (Configuration):**
    *   انسخ ملف المثال: `cp .env.example .env`
    *   **هام:** قم بتعبئة المتغيرات الحرجة في قسم `SECURITY` و `LEGAL_COMPLIANCE`.
    *   حدد استراتيجية التوجيه: `AI_ROUTING_STRATEGY=hybrid`.

3.  **إطلاق النظام (Launch):**
    ```bash
    # تنظيف البيئة السابقة
    bash deployment/00_wipe.sh
    
    # تشغيل البنية التحتية (DB, Redis)
    bash deployment/01_infra.sh
    
    # تحميل نماذج الذكاء الاصطناعي
    bash deployment/02_brain.sh
    
    # حقن قوانين الامتثال
    python3 deployment/03_compliance.py
    
    # تشغيل التطبيق
    bash deployment/04_app.sh
    ```

---

## 3. استراتيجية الذكاء الهجين (Hybrid Intelligence Strategy)

يعتمد النظام "منطق التوجيه الذكي" (Adaptive Routing Logic) لضمان السيادة والكفاءة:

| السيناريو (Scenario) | المحرك المستخدم (Engine) | السبب (Reasoning) |
| :--- | :--- | :--- |
| **تحليل وثائق سرية / بيانات شخصية** | **Local (Ollama/Qwen)** | (السيادة) منع خروج البيانات من السيرفر المحلي نهائياً. |
| **بحث في الويب / تلخيص عام** | **Cloud (Gemini)** | (السرعة) الاستفادة من قدرات Google في البحث المباشر. |
| **كشف التزييف العميق** | **Local (DeepSafe)** | (الأمن) تحليل البصمة الرقمية محلياً. |
| **الترجمة والتدقيق اللغوي** | **Hybrid** | مسودة أولية محلياً، وتحسين الصياغة سحابياً (بدون بيانات حساسة). |

---

## 4. الامتثال القانوني (Legal Compliance)

تمت برمجة القوانين اليمنية داخل النظام (Code as Law):

1.  **حماية الخصوصية (المادة 255 - عقوبات):**
    *   يتم تفعيل `RLS` (Row Level Security) في قاعدة البيانات لعزل بيانات كل مستأجر.
    *   يتم تشفير الحقول الحساسة باستخدام `ENCRYPTION_KEY`.

2.  **المصداقية ومكافحة التضليل (قانون الصحافة):**
    *   يمنع النظام توليد أي محتوى إخباري بدون "مصدر" (Citation).
    *   يتم وسم المحتوى المولد بـ AI بعلامة مائية رقمية.

3.  **الأمن القومي:**
    *   فلاتر الكلمات المحظورة (`AI_SAFETY_LEVEL=strict`) تمنع معالجة أو توليد محتوى يثير النعرات الطائفية أو يهدد السلم الاجتماعي.

---

## 5. خطة التعافي من الكوارث (Disaster Recovery)

في حال انقطاع الإنترنت الدولي (Internet Blackout):

1.  **تفعيل الوضع المحلي الصارم:**
    *   قم بتغيير المتغير: `AI_ROUTING_STRATEGY=strict_local`.
    *   أعد تشغيل الحاويات: `docker-compose restart backend`.
2.  **النتيجة:**
    *   سيعمل النظام بكفاءة 100% للمهام الداخلية (الأرشيف، التحليل، الكتابة).
    *   ستتوقف ميزات البحث في الويب (Gemini/Google) فقط.
    *   تبقى البيانات آمنة ومتاحة محلياً.

---
**اعتماد:**
*كبير مهندسي النظم (Chief Architect)*
