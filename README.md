
# 🇾🇪 RaidanPro: منصة السيادة الرقمية (Sovereign Platform)
**الإصدار:** 3.1 (Hybrid Edition)
**التصنيف:** بنية تحتية حرجة

نظام **RaidanPro** هو منصة استخبارات وتحليل بيانات سيادية، مصممة للعمل داخل الجمهورية اليمنية. يعتمد النظام **"الهندسة الهجينة" (Hybrid Architecture)**، حيث تعمل محركات الذكاء الاصطناعي (Ollama) بشكل أصلي (Native) على نظام التشغيل للاستفادة القصوى من عتاد السيرفر، بينما تعمل الخدمات المساندة (قواعد البيانات، الواجهات) داخل حاويات Docker معزولة.

---

## 📋 المتطلبات المسبقة (Prerequisites)

1. **نظام التشغيل:** Debian 13 (Trixie) أو Ubuntu 24.04 LTS.
2. **الموارد:**
   - **RAM:** 16GB كحد أدنى (يفضل 32GB لتشغيل Qwen:14b براحة).
   - **CPU:** معالج يدعم AVX2 (لأداء Ollama Native).
   - **Storage:** 100GB NVMe SSD (لقواعد البيانات والموديلات).
3. **الشبكة:** اتصال إنترنت مستقر لعملية التثبيت، ومنفذين 80 و 443 مفتوحين.
4. **الصلاحيات:** وصول Root كامل (`sudo su`).

---

## 🚀 بروتوكول التثبيت السريع (Quick Start)

تم دمج عملية التثبيت في "معالج تكتيكي" واحد يقوم بكل شيء بدءاً من تنظيف السيرفر وحتى تشغيل الواجهة.

1. **نقل ملفات النظام إلى السيرفر:**
   ```bash
   git clone https://github.com/RaidanPro/sovereign-core.git /opt/raidan
   cd /opt/raidan
   ```

2. **تشغيل معالج الإطلاق (Launch Wizard):**
   يقوم هذا السكربت بتنفيذ بروتوكول "الأرض المحروقة" (تنظيف أي تثبيت سابق)، تثبيت Docker و Ollama، وإطلاق واجهة الإعداد.
   ```bash
   chmod +x launch_wizard.sh
   ./launch_wizard.sh
   ```

3. **الوصول للمعالج الرسومي:**
   بعد انتهاء السكربت، سيظهر لك رابط الوصول:
   `http://SERVER_IP:8080`

---

## 🛠️ تفاصيل مراحل التثبيت (Installation Phases)

### المرحلة 0: التطهير الشامل (Total Purge)
يقوم `launch_wizard.sh` بمسح أي بقايا سابقة لضمان بيئة نظيفة:
- إيقاف وحذف كافة حاويات Docker.
- إيقاف خدمة Ollama القديمة.
- تحرير المنافذ 80, 443, 11434.

### المرحلة 1: التأسيس الهجين (The Hybrid Shift)
- **Ollama Native:** يتم تثبيت Ollama مباشرة على الـ Host (`systemd service`)، ويتم ضبطه للاستماع على العنوان `0.0.0.0:11434` ليتمكن الـ Backend داخل الدوكر من الاتصال به. هذا يضمن وصول الموديل لكرت الشاشة (GPU) أو المعالج (CPU) دون طبقات عزل تبطئ الأداء.
- **Docker Network:** إنشاء شبكة سيادية `sovereign_net` بنطاق `172.28.0.0/16`.

### المرحلة 2: المعالج الرسومي (Tactical Wizard)
عند فتح الرابط `http://SERVER_IP:8080`:
1. **الفحص (Diagnostics):** يتحقق من توافق Debian 13 والذاكرة.
2. **الإعدادات (Config):** إدخال اسم النظام، النطاق (Domain)، ومفاتيح التشفير.
3. **السيادة (Legal):** الموافقة الإلكترونية على الالتزام بالقانون اليمني (شرط أساسي لتفعيل الموديلات).
4. **التنفيذ (Execution):** يتم الاتصال عبر WebSocket لتنفيذ الأوامر الحية.

### المرحلة 3: الحقن والتشغيل
- يقوم المعالج بسحب الموديلات (`qwen2.5`, `nomic-embed-text`) إلى الـ Host مباشرة.
- يتم حقن "الدستور الرقمي" (تعليمات النظام الصارمة) في ملف إعدادات الموديل.
- يتم تشغيل حاويات الخدمات (`Postgres`, `Redis`, `Backend`, `Frontend`) بعناوين IP ثابتة.

---

## 🔧 الهيكلية الشبكية (Network Topology)

يعمل النظام بتوزيع عناوين IP ثابتة لضمان استقرار الاتصال الداخلي:

| الخدمة | العنوان الداخلي (Docker IP) | المنفذ | ملاحظات |
| :--- | :--- | :--- | :--- |
| **Host Gateway** | `172.28.0.1` | - | بوابة الاتصال بالنظام الأصلي |
| **Native Ollama** | `172.28.0.1` | `11434` | يعمل على الـ Host، متاح للحاويات |
| **Traefik Proxy** | `172.28.0.2` | `80/443` | بوابة الويب الموحدة |
| **PostgreSQL** | `172.28.0.10` | `5432` | قاعدة البيانات المشفرة |
| **Raidan API** | `172.28.0.30` | `8000` | المحرك الخلفي |
| **Raidan UI** | `172.28.0.32` | `3000` | واجهة المستخدم |

---

## ⚠️ استكشاف الأخطاء (Troubleshooting)

**مشكلة: المعالج لا يتصل بـ Ollama**
- تأكد أن خدمة Ollama تعمل على الـ Host: `systemctl status ollama`.
- تأكد من تعديل ملف الخدمة للسماح بالاتصال الخارجي (تم ذلك في `launch_wizard.sh`، تحقق من `OLLAMA_HOST=0.0.0.0`).

**مشكلة: تعارض الحزم (dpkg lock)**
- أعد تشغيل `launch_wizard.sh`، فهو مصمم لقتل العمليات العالقة وحذف ملفات القفل.

**مشكلة: فشل الـ DNS**
- تأكد أن النطاق `raidan.pro` يشير إلى IP السيرفر العام في Cloudflare.

---

**حقوق الملكية:**
نظام RaidanPro مملوك ومشفر لصالح الجهة المشغلة. يمنع نسخ الكود المصدري خارج البيئة السيادية.
